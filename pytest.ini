[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Coverage configuration (run with: pytest --cov)
# addopts = --cov=api --cov-report=term-missing --cov-report=html
# Note: Coverage is opt-in. Run with --cov flag when needed.
